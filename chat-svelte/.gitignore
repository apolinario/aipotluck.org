.DS_Store
node_modules
/build
/.svelte-kit
/package
.env
.env.*
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
SECRET_CONFIG
.idea
!.env.ci
gcp-*.json
# Anchored: only the upstream MongoMemoryServer data dir at repo root, NOT our
# Postgres persistence layer in src/lib/server/db (a bare `db` matched both).
/db
models/*
!models/add-your-models-here.txt
.claude/
.vercel

# Playwright mobile-viewport screenshots (dev-only)
.shots

# calque (drift nose): commit the adjudicated registry — it's shared memory —
# but ignore the local calibration telemetry it writes alongside it.
.calque/fires.jsonl
.calque/weights.json

# plancheck debug scratch
.plancheck-debug.json
